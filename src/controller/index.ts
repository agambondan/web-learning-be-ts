import UserController from './userController';
import CategoryController from './categoryController';

const Controller = {
    UserController,
    CategoryController
}

export default {UserController, CategoryController};
// export default Controller;
// export default [].concat(UserController, CategoryController);